{
    "framework": "nextjs",
    "buildCommand": "cd ../../../ && npx turbo run build --filter=hashnode",
    "installCommand": "pnpm install",
    "devCommand": "cd ../../../ && pnpm dev --filter=hashnode"
}
